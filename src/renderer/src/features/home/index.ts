/**
 * Home feature — public API.
 *
 * FSD Rule: features are self-contained modules. The app layer
 * only ever imports through this barrel export.
 */
export { HomePage } from './ui/HomePage/HomePage';
